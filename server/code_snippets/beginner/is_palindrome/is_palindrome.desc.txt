Check whether a string reads the same forwards and backwards. The mirror index is calculated as len(s) - i, but it should be len(s) - 1 - i. Without the -1, the comparison goes out of bounds or checks the wrong character.